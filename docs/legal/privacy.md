# Privacy Policy

Immune-Brain is a local Codex plugin. It does not send data to a service owned
by this repository.

The plugin reads and writes local project files only when a user asks Codex to
run a workflow that needs that access. Any network access, repository hosting,
or model-provider logging is controlled by the user's Codex environment and
configured tools, not by this plugin.
