{
  "kind": "pi-agent-benchmark",
  "schemaVersion": 1,
  "version": 3,
  "targetKind": "skill",
  "targetName": "imm-brainstorm",
  "runner": {
    "type": "pi-agent",
    "model": "antigravity/gemini-3.6-flash",
    "subagentType": "general-purpose",
    "isolated": true,
    "parallel": true
  },
  "workspace": {
    "sourcePath": "tests/fixtures/immune-brain-benchmark-workspace",
    "setupMode": "copy",
    "preserve": "on-failure"
  },
  "targetProvisioning": {
    "mode": "pi-worktree-local-skill"
  },
  "metrics": {
    "required": [
      "scenario_status",
      "question_count",
      "tool_uses",
      "reported_tokens",
      "duration_ms",
      "advisory_metrics"
    ],
    "cost": "unavailable_by_host"
  },
  "verifiers": {
    "commands": [
      "bun test tests",
      "test ! -d docs/specs && test ! -d docs/plans",
      "test ! -d upstreams",
      "bun -e 'const glob = new Bun.Glob(\"**/*\"); let count = 0; for await (const p of glob.scan({ cwd: \".\", onlyFiles: true })) if (!p.split(\"/\").includes(\".git\")) count++; if (count >= 180) throw new Error(String(count))'"
    ]
  },
  "notes": [
    "This focused config measures Brainstorm exhaustive frontier behavior separately from the four-route plugin baseline.",
    "Every scenario runs through a read-only isolated Pi Agent and should leave no Spec, Plan, CONTEXT.md, or implementation edit.",
    "Launch all five scenarios in one parallel batch with antigravity/gemini-3.6-flash.",
    "Record provider billing cost as unavailable_by_host; do not estimate it."
  ],
  "scenarios": [
    {
      "id": "dependent-frontier-blocks-downstream",
      "title": "Hold downstream decisions until dependencies resolve",
      "purpose": "Verify that Brainstorm asks the complete currently unblocked frontier while keeping dependent retention decisions blocked.",
      "userInput": "Use the local Pi Skill `imm-brainstorm` to frame this request in the fixture workspace: improve account cancellation. Whether data retention needs discussion depends entirely on whether cancellation means ending a subscription or deleting the account. Inspect only CONTEXT.md, README.md, and docs/solutions first. Ask the complete currently unblocked frontier and hold downstream retention questions until their prerequisite is decided. Do not create or edit any files.",
      "successChecklist": [
        "The response asks the currently unblocked question about the meaning of cancellation.",
        "The response does not ask a downstream data-retention question before its prerequisite is answered.",
        "The question is numbered and includes a recommended answer.",
        "The response remains in Brainstorm and no workspace file is created or changed."
      ]
    },
    {
      "id": "independent-complete-frontier",
      "title": "Ask the complete independent frontier together",
      "purpose": "Verify that Brainstorm surfaces every independent unresolved decision in the same dependency-aware round.",
      "userInput": "Use the local Pi Skill `imm-brainstorm` to frame a lightweight internal status dashboard. The intended audience and the measurable success target are both missing, and either answer can be decided independently. Inspect only CONTEXT.md and README.md first. Ask the complete currently unblocked frontier together with numbered recommendations and allow bulk acceptance. Do not create or edit any files.",
      "successChecklist": [
        "The response asks about both intended audience and measurable success target in the same round.",
        "Each question is numbered and includes a recommended answer.",
        "The response allows bulk acceptance with explicit exceptions.",
        "No workspace file is created or changed."
      ]
    },
    {
      "id": "scenario-qualified-frontier",
      "title": "Use a scenario only when it distinguishes behavior",
      "purpose": "Verify that a concrete scenario qualifies as an unresolved frontier branch when it distinguishes a lifecycle boundary.",
      "userInput": "Use the local Pi Skill `imm-brainstorm` to frame a `pause workspace` feature. It is unclear whether people with an already-open session may keep working after an admin pauses the workspace. Inspect only CONTEXT.md and README.md first. Include a concrete user scenario in the currently unblocked frontier because it distinguishes the lifecycle boundary. Do not create or edit any files.",
      "successChecklist": [
        "The response asks a concrete scenario question involving an already-open session and an admin pause.",
        "The scenario distinguishes the lifecycle or scope boundary instead of requesting generic implementation detail.",
        "The question is numbered and includes a recommended answer.",
        "No workspace file is created or changed."
      ]
    },
    {
      "id": "recommendation-adoption-continues",
      "title": "Continue traversal after recommendations are adopted",
      "purpose": "Verify that adopting the current recommendations settles those nodes but does not prematurely complete Brainstorm while downstream branches remain.",
      "userInput": "Continue the local Pi Skill `imm-brainstorm` session for an admin-only CSV export. In the prior round you asked who can export and which rows are included; I adopted both recommendations: admins only, current visible rows only. Adopting recommendations closes only the current frontier nodes. Continue traversing any newly unlocked current-goal branches, including failure behavior and verification, with numbered recommendations. Do not create or edit any files.",
      "successChecklist": [
        "The response treats the adopted audience and row-scope recommendations as settled without asking them again.",
        "The response continues with newly unlocked current-goal questions such as download failure behavior or observable verification.",
        "Each newly surfaced question is numbered and includes a recommended answer.",
        "The response does not declare the Brainstorm complete or route to Planner while those branches remain open, and no workspace file is created or changed."
      ]
    },
    {
      "id": "rejected-decision-unmet-condition",
      "title": "Keep an unmet rejected decision closed",
      "purpose": "Verify that recorded reconsideration conditions prevent generic re-litigation when repository evidence has not changed.",
      "userInput": "Use the local Pi Skill `imm-brainstorm` to frame a proposal for a shared generic dispatcher in this fixture. Inspect docs/solutions/rejected-generic-dispatcher.md plus CONTEXT.md and README.md. The fixture still has only one host adapter and no repeated dispatch drift. Apply the recorded rejection and reconsider_if conditions; do not ask a generic `what changed?` question when no condition is met. Do not create or edit any files.",
      "successChecklist": [
        "The response treats the generic dispatcher as a current constraint or non-goal.",
        "The response cites the unmet reconsideration evidence instead of reopening the decision.",
        "The response asks no generic `what changed?` question and does not route to Planner.",
        "No workspace file is created or changed."
      ]
    }
  ]
}
