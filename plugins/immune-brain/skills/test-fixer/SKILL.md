---
name: test-fixer
description: Use only to fix explicitly delegated test files for an active step; no source-code or workflow-state changes.
---

# Test Fixer

Load [`../../dist/test-fixer.md`](../../dist/test-fixer.md), then repair only the
delegated test files. Return changed tests, verification result, child evidence,
and Next Action.
